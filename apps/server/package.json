{
  "name": "@er-diagram/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:integration": "pnpm -w build:resource-workers && vitest run --config ../../vitest.integration.config.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@er-diagram/contracts": "workspace:*",
    "@er-diagram/core": "workspace:*",
    "@er-diagram/source-transform": "workspace:*",
    "@er-diagram/storage-sqlite": "workspace:*",
    "@fastify/helmet": "13.0.0",
    "fastify": "5.12.1",
    "yauzl": "3.4.0",
    "yazl": "3.3.1"
  },
  "devDependencies": {
    "@er-diagram/test-fixtures": "workspace:*",
    "@types/yauzl": "3.4.0",
    "@types/yazl": "3.3.1",
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  }
}
