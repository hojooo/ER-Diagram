{
  "name": "er-diagram",
  "version": "0.0.0",
  "author": "hojooo",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=24 <25",
    "pnpm": ">=10 <11"
  },
  "scripts": {
    "architecture:check": "node scripts/check-architecture.mjs",
    "build:resource-workers": "pnpm --filter @er-diagram/contracts build && pnpm --filter @er-diagram/core build && pnpm --filter @er-diagram/source-transform build && pnpm --filter @er-diagram/storage-sqlite build && pnpm --filter @er-diagram/server build",
    "build": "pnpm -r --if-present build && pnpm workspace:imports:check",
    "check": "pnpm docs:check && pnpm format:check && pnpm lint && pnpm typecheck",
    "ci:verify": "pnpm check && pnpm architecture:check && pnpm test:unit && pnpm test:security && pnpm test:integration && pnpm build && pnpm licenses:check",
    "docs:check": "node scripts/check-docs.mjs",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "licenses:check": "node --test scripts/check-licenses.test.mjs && node scripts/check-licenses.mjs",
    "lint": "biome lint .",
    "security:sinks": "node scripts/check-web-security-sinks.mjs",
    "storage:backup": "pnpm build:resource-workers && node apps/server/dist/volume-recovery-cli.js backup",
    "storage:migrate": "pnpm build:resource-workers && node apps/server/dist/volume-recovery-cli.js migrate",
    "storage:restore": "pnpm build:resource-workers && node apps/server/dist/volume-recovery-cli.js restore",
    "test": "vitest run",
    "test:container": "node scripts/test-container.mjs",
    "test:e2e": "pnpm --filter @er-diagram/contracts build && pnpm --filter @er-diagram/core build && pnpm --filter @er-diagram/source-transform build && pnpm --filter @er-diagram/test-fixtures build && playwright test",
    "test:e2e:security": "pnpm build:resource-workers && playwright test --config playwright.security.config.ts",
    "test:integration": "pnpm build:resource-workers && vitest run --config vitest.integration.config.ts",
    "test:m1-gate": "pnpm --filter @er-diagram/test-fixtures test test/synthetic-fixtures.test.ts && pnpm --filter @er-diagram/server test:integration milestone-1-gate && pnpm test:e2e milestone-1-gate",
    "test:m2-gate": "pnpm --filter @er-diagram/test-fixtures test test/sql-interchange-gate-fixtures.test.ts && pnpm --filter @er-diagram/core test test/sql-interchange-gate.test.ts && pnpm --filter @er-diagram/server test:integration milestone-2-gate && pnpm test:e2e milestone-2-gate",
    "test:m3-gate": "pnpm --filter @er-diagram/test-fixtures test test/visual-command-gate-fixtures.test.ts && pnpm --filter @er-diagram/contracts test test/visual-command.test.ts && pnpm --filter @er-diagram/source-transform test test/visual-command-gate.test.ts && pnpm --filter @er-diagram/server test:integration milestone-3-gate && pnpm test:e2e milestone-3-gate",
    "test:perf": "node scripts/run-perf-tests.mjs",
    "test:security": "pnpm security:sinks && pnpm build:resource-workers && vitest run --config vitest.security.config.ts",
    "test:unit": "vitest run --config vitest.config.ts",
    "typecheck": "pnpm -r --if-present typecheck",
    "workspace:imports:check": "node scripts/check-workspace-imports.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.10",
    "@er-diagram/contracts": "workspace:*",
    "@er-diagram/core": "workspace:*",
    "@er-diagram/source-transform": "workspace:*",
    "@er-diagram/storage-sqlite": "workspace:*",
    "@er-diagram/test-fixtures": "workspace:*",
    "@playwright/test": "1.62.1",
    "@types/node": "24.10.0",
    "dependency-cruiser": "18.2.0",
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  }
}
